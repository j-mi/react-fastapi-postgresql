import Typography from '@material-ui/core/Typography';

export const Header = () => {
    return (
        <Typography p={3} variant="h1" component="div" > URL snipper </Typography>
    );
  };
  

  
  